# Server Configuration
server.port=${PORT:8080}

# MySQL DataSource Configuration - Files.io
spring.datasource.url=jdbc:mysql://7m3sv.h.filess.io:61001/filedropx_clocksitto?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=filedropx_clocksitto
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Spring Security
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:123321}

# File Upload Configuration
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# OAuth2 Configuration for Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:61397727162-a3cquuur88kocdmjq0sbvlqad03rff1b.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=${BASE_URL:https://your-app-name.onrender.com}/login/oauth2/code/google

# OAuth2 Configuration for GitHub
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:Ov23li34HjQEwucygG0O}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET:ddfc17d604c8b1c1897d89bacfa0e8d61dd3fde1}
spring.security.oauth2.client.registration.github.scope=user:email
spring.security.oauth2.client.registration.github.redirect-uri=${BASE_URL:https://your-app-name.onrender.com}/login/oauth2/code/github

# Production Profile Settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Logging Configuration
logging.level.org.springframework=${LOG_LEVEL:INFO}
logging.level.com.example.FileSharing=${APP_LOG_LEVEL:INFO}
logging.level.org.hibernate.SQL=${SQL_LOG_LEVEL:WARN}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${BINDER_LOG_LEVEL:WARN}

# Error handling
server.error.include-stacktrace=on_param
server.error.include-message=always
